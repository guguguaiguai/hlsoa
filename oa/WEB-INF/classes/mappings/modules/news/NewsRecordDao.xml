<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hls.ws.modules.news.dao.NewsRecordDao">
    
	<sql id="newsRecordColumns">
		a.ID AS "id",
		a.NEWS_ID AS "news.id",
		a.USER_ID AS "user.id",
		a.READ_FLAG AS "readFlag",
		u.name AS "user.name",
		o.name AS "user.office.name"
	</sql>
	
	<sql id="newsRecordJoins">
		JOIN sys_user u ON u.id = a.user_id
		JOIN sys_office o ON o.id = u.office_id
	</sql>
    
	<select id="get" resultType="NewsRecord">
		SELECT 
			<include refid="newsRecordColumns"/>
		FROM NEWS_RECORD a
		<include refid="newsRecordJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="NewsRecord">
		SELECT 
			<include refid="newsRecordColumns"/>
		FROM NEWS_RECORD a
		<include refid="newsRecordJoins"/>
		WHERE 1=1
		<if test="news != null and news.id != null and news.id != ''">
			AND news_id = #{news.id}
		</if>
		ORDER BY a.read_flag ASC
	</select>
	
	<select id="findAllList" resultType="NewsRecord">
		SELECT 
			<include refid="newsRecordColumns"/>
		FROM NEWS_RECORD a
		<include refid="newsRecordJoins"/>
		WHERE 1=1
		ORDER BY a.read_flag ASC
	</select>
	<!-- 根据公告ID与当前登录者ID查询是否已读所选公告信息 -->
	<select id="newsReadCount" resultType="Long">
	    SELECT 
	    	count(1)
	    FROM news_record a
	    where a.user_id=#{currentUser.id} and a.news_id=#{wsNews.id}
	</select>
	
	<insert id="insert">
		INSERT INTO NEWS_RECORD(
			ID,
			NEWS_ID,
			USER_ID,
			READ_FLAG
		) VALUES (
			#{id},
			#{wsNews.id},
			#{user.id},
			#{readFlag}
		)
	</insert>
	
	<insert id="insertAll" parameterType="List">
		INSERT INTO NEWS_RECORD(
			ID,
			NEWS_ID,
			USER_ID,
			READ_FLAG	
		)
		<foreach collection="list" item="e" separator=" UNION ALL ">
			SELECT
				#{e.id},
				#{e.wsNews.id},
				#{e.user.id},
				#{e.readFlag}
			FROM dual
		</foreach>
	</insert>
</mapper>